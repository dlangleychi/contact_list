[project]
name = "contact-list"
version = "0.1.0"
description = ""
authors = [
    {name = "Daniel Langley",email = "dlangley812@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask (>=3.1.3,<4.0.0)",
    "psycopg2 (>=2.9.12,<3.0.0)"
]

[tool.poetry]
packages = [{include = "contact_list", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
